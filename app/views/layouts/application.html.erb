<!DOCTYPE html>
<html>
<head>
  <title>Smitten</title>
  <link href='http://fonts.googleapis.com/css?family=Coustard' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="http://code.jquery.com/mobile/1.3.2/jquery.mobile-1.3.2.min.css" />
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>

  <div data-role="page" data-theme='a'>
    <div data-role="header">
       <h1>Smitten </h1>

      <% if current_page?(root_path) %>
      <!-- the following links get rendered if current page is the root-->
        <%= link_to "New Spark", new_spark_path, data: { transition: "slide", direction: "reverse" }, :id => "swiperight" %>
        <%= link_to "Dashboard", dashboard_path, data: { transition: "slide" }, :id => "swipeleft" %>
      <% elsif current_page?(new_spark_path)%>
      <!-- the following link get rendered if current page is to make a new spark-->
        <%= link_to "Home", root_path, data: { transition: "slide" }, :id => "sparkhome" %>
      <% elsif current_page?(dashboard_path)%>
      <!-- the following link get rendered if current page is the dashboard-->
        <%= link_to "Home", root_path, data: { transition: "slide",direction: "reverse" }, :id => "dashhome" %>
      <% end %>
    </div>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <div data-role="content">
        <%= yield %>
      </div>

</div>
<script src="http://code.jquery.com/mobile/1.3.2/jquery.mobile-1.3.2.min.js"></script>
<script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js"></script>
</body>
</html>
