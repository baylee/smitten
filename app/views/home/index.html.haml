%h1 Welcome to the index page!
%p Find me in app/views/home/index.html.haml

- if user_signed_in?
  %h4 Sparks near your past locations:
  %ul
    - if !@nearsparks[0].nil?
      -@nearsparks.each do |x|
        / -binding.pry
        %li
          = x.content
          = x.created_at.advance(:hours => -7).strftime('%B %e, %Y at %l:%M %p')
          = x.user_id





